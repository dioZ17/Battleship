#include <iostream>
#include <ctime>
#include <cstdlib>
#include <cstring>
#include "battleships.h"

using namespace std;

int main(){
	
	
	
	
	
	
	
	
/*	


int i,j;
	bool adjacent;
	
	int shots[2];

	
	
	
	
	
	
	
	
	Player player
	
	("DioZ");
	Player player("PC");
	
	Board bord;
	Board op_map;
	Game game;

	
	
	
	
	
	
	
	
	
	
	op_map.placeAllShips();
	bord.placeAllShips();
	bord.drawBoards(&op_map);
	shots[0] = 0;shots[1] = 0;
	player.fire(&op_map, shots);
	shots[0] = 1;shots[1] = 1;
	
	
	player.fire(&op_map, shots);
	shots[0] = 2;shots[1] = 2;
	player.fire(&op_map, shots);
	shots[0] = 3;shots[1] = 3;
	player.fire(&op_map, shots);
	shots[0] = 4;shots[1] = 4;
	player.fire(&op_map, shots);
	shots[0] = 5;shots[1] = 5;
	player.fire(&op_map, shots);
	shots[0] = 6;shots[1] = 6;
	player.fire(&op_map, shots);

		
	bord.drawBoards(&op_map);
	game.getInputQuordinates();
	game.getYNInput();
	*/
	Game game;
	game.mainGame();	
}

